//! Base functionality for `Matrix`: construction, shape, equality and
//! display. Each exercise gets its own sibling module (e.g. `matrix::ex00`)
//! once it's implemented.

mod copy;
mod eq;
mod new;
mod repr;
mod shape;
