[package]
name = "matrix"
version = "0.1.0"
edition = "2021"

[lib]
name = "_core"
path = "src/rust/lib.rs"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module", "multiple-pymethods"] }
